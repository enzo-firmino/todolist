import React from 'react';
import {Footer} from './index';

export default {
    title: 'components/Header',
    component: Footer,
}

export const default_value = () => <Footer/>;
export const initialized = () => <Footer/>;
