import React from 'react';
import {CreateTodo} from './index';

export default {
    title: 'components/CreateTodo',
    component: CreateTodo,
}

export const default_value = () => <CreateTodo/>;
export const initialized = () => <CreateTodo/>;