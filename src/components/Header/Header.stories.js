import React from 'react';
import {Header} from './index';

export default {
    title: 'components/Header',
    component: Header,
}

export const default_value = () => <Header/>;
export const initialized = () => <Header/>;